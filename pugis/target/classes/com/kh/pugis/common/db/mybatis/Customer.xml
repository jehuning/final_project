<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Customer">

	<resultMap type="com.kh.pugis.service.consulting.domain.CustomerInfo" id="result">
		<result property="customer_id" column="customer_id" />
		<result property="customer_name" column="customer_name" />
	
	</resultMap>
	
	<select id="countSelect" parameterType="com.kh.pugis.service.consulting.domain.CustomerInfo" resultType="int">
		SELECT
		 COUNT(0)
		FROM
			customer_info
		WHERE
			customer_address= #{customer_address}
			and customer_grade = #{customer_grade}		
	</select>

	<select id="selectList" parameterType="hashmap" resultMap="result">
		SELECT
		   customer_id, customer_name
		FROM
			(SELECT ROWNUM NUM, A.customer_id,A.customer_name
			FROM customer_info A
			WHERE 
				customer_address= #{customer_address}
				and customer_grade = #{customer_grade}
			)
		WHERE NUM BETWEEN #{page_begin} AND #{page_end}
		
	</select>
		
</mapper>
