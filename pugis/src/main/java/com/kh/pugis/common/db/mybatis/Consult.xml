<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Consult">

	
	<insert id="insertRcSche" parameterType="hashmap">
		insert into 
			   consult_schedule(CONSULT_SCHDULE_ID,CONSULT_CODE,CUSTOMER_ID,CONSULT_DATE)
		values(CON_SCHE_SEQ.NEXTVAL,'RG',#{customerId},#{scheduleDay})
			
			
	</insert>
	<select id="allConsult" parameterType="hashmap" 
			resultType="com.kh.pugis.service.consulting.domain.Consult"><!-- sql수정필요 -->
		SELECT
		   CUSTOMER_ID,CONSULT_DATE,SCHEDULE_NAME
		FROM
			CONSULT_SCHEDULE
		WHERE
		 	CONSULT_DATE BETWEEN #{start_date} AND #{finish_date}
		
	</select>	
	<select id="regularConsult" parameterType="com.kh.pugis.service.consulting.domain.ConsultScheduleDate" 
			resultType="com.kh.pugis.service.consulting.domain.Consult"><!-- sql수정필요 -->
		SELECT
		   CUSTOMER_ID,CONSULT_DATE,SCHEDULE_NAME
		FROM
			CONSULT_SCHEDULE
		WHERE
			CONSULT_CODE = 'RG' 
		AND
		 	CONSULT_DATE BETWEEN #{start_date} AND #{finish_date}
		
	</select>		
	<select id="marketingConsult" parameterType="com.kh.pugis.service.consulting.domain.ConsultScheduleDate" 
			resultType="com.kh.pugis.service.consulting.domain.Consult"><!-- sql수정필요 -->
		SELECT
		   CUSTOMER_ID,CONSULT_DATE,SCHEDULE_NAME
		FROM
			CONSULT_SCHEDULE
		WHERE
			CONSULT_CODE = 'MK' 
		AND
		 	CONSULT_DATE BETWEEN #{start_date} AND #{finish_date}
		
	</select>	
		
</mapper>