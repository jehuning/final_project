<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Schedule">

	
		
	<select id="selectToday" parameterType="com.kh.pugis.service.consulting.domain.Schedule" 
			resultType="com.kh.pugis.service.consulting.domain.Schedule">
		SELECT
		   SCHEDULE_ID,EMP_ID,SCHEDULE_DATE,SCHEDULE_DEPT,SCHEDULE_NAME
		FROM
			EMP_SCHEDULE
		WHERE
			emp_id = #{emp_id} 
		AND
			SCHEDULE_DATE = #{schedule_date}
		
	</select>
	
	<select id="selectList" parameterType="hashmap" 
			resultType="com.kh.pugis.service.consulting.domain.Schedule">
		SELECT
		   SCHEDULE_ID,EMP_ID,SCHEDULE_DATE,SCHEDULE_DEPT,SCHEDULE_NAME
		FROM
			EMP_SCHEDULE
		WHERE
			emp_id = #{emp_id} 
		AND
		 	SCHEDULE_DATE BETWEEN #{sDate} AND #{fDate}
		
	</select>
		
</mapper>